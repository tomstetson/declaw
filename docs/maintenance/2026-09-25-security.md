# Security maintenance — 2026-09-25

Updated the root, UI and extension dependency graph to patched compatible releases, including tar 7.5.22, Sharp 0.35.4, PDF.js 6.3.289, Hono 4.13.9, fast-xml-parser 5.11.1, Undici, WebSocket, minimatch, protobufjs and the OpenTelemetry SDK. Node 22.13 or newer is required. Workspace aliases now resolve the renamed DeClaw root correctly. Sharp decoding and Discord voice channel types were adapted to the updated APIs.

## Pi security backport

The application depends on the Pi 0.55 API. Upgrading that API wholesale breaks this fork, so the four affected implementation files receive a checked-in, hash-bound backport of upstream security changes:

- Private extension download/cache directories and containment checks for every Git install scope; raw, percent-encoded and Windows traversal forms are rejected (GHSA-jfgx-wxx8-mp94).
- Auth files receive owner-only permissions at creation, before credential bytes are written (GHSA-r95r-rj6r-c39x).
- Exported HTML restricts link schemes and escapes attributes, including quoted payloads, in both Pi and DeClaw's renderer (GHSA-7v5m-pr3q-6453).

Upstream references: [private cache](https://github.com/earendil-works/pi/commit/ea3465a8e371a12d0167a06b60f93878e3a3df44), [Git containment](https://github.com/earendil-works/pi/commit/a98e087e5d08ea2a536bf73dbb0aebb87c3ef72e), [auth creation](https://github.com/earendil-works/pi/commit/135fb545f99106a4a249274f129b90bc0a77d347), and [HTML links](https://github.com/earendil-works/pi/commit/6cb23f9b5d5b6d1747672f535b167d0d809ac010).

pnpm applies the checked-in patch. npm installations run `scripts/apply-pi-security.mjs`, which accepts only the exact 0.55.0 upstream or already-patched file hashes, validates all inputs before writing, and fails on unknown content. The published file list includes the installer, patched files, manifest and upstream MIT license. Installations that disable lifecycle scripts must explicitly run this installer before use. The prepare step now preserves an existing Git hooks configuration, including global security hooks.

## Verification and residual alerts

Frozen pnpm installation, build, TypeScript checking and focused lint passed. Nineteen HTML attack/control cases passed, including eight cases reproduced as failing before the attribute fix. Three executable backport tests cover private cache permissions, traversal aliases and all auth creation paths. Twenty-two nearby media/Discord tests passed. The npm installer passed pristine-file application, repeat-install and unexpected-content rejection checks; package dry-run confirmed every backport asset ships.

Reproduce the boundary checks with `node --test scripts/pi-security-backport.test.mjs` and `pnpm exec vitest run src/auto-reply/reply/export-html/template.security.test.ts`; run `pnpm install --frozen-lockfile`, `pnpm tsgo` and `pnpm build` for compatibility.

The raw version-based audit still reports five advisory entries: one high and two low Pi entries mitigated by the verified backport, plus two moderate entries (uuid and markdown-it). This is not a raw zero-alert graph. GitHub's version-based Pi alert is expected until the fork migrates APIs or upstream ships a compatible release; do not dismiss it without checking the installed file hashes and regressions above. Live external-provider integrations were not exercised.

Rollback: revert this maintenance commit and reinstall its parent lockfile in a separate environment. Keep the patched environment available until the rollback is verified; the older graph restores known vulnerabilities.

The production audit hook now preserves raw scanner counts and rejects every
unrecognized high/critical advisory. Its sole exception requires the exact Pi
0.55.0 advisory, all reachable installed/vendored file hashes, and the executable
boundary tests. Missing scan data, unexpected versions or unpatched bytes fail
closed. Run `node --test scripts/audit-production.test.mjs` and
`node scripts/audit-production.mjs` to verify the gate. The pre-existing oxlint
1.50 toolchain is pinned to avoid unrelated new lint rules. Baileys asynchronous
socket cleanup now handles rejected close promises; full check/docs and nearby
login tests pass after the compatibility changes.

The full CI matrix exposed an existing reversed tslog severity mapping that
suppressed ordinary messages under trace logging and could filter errors at
restrictive levels. A regression proves trace/warn/error/fatal behavior, and
66 logging/messaging tests pass with the corrected thresholds. Windows test
fixtures now use its temporary directory and an actually relative media root.
The media-root validation itself is unchanged.
